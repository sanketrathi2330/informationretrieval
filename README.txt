To run the operation run the file searchengine.py.(in python 2.x version)

NOTE: all the files queryOperation.py, porterStemmer.py, indexing.py, etc needs to be in the same directory.

after running searchengine.py type 'f' and specify the file names of files saperated by spaces
or type 'd' and specify the name of the adress of the directory containing the files w.r.t the working directory
to create the indexFile.

NOTE: The operations(filename) requires the files to be present in the same directory as the codefiles

Works for any file containing text as its data.
